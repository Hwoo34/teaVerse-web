# 개발 컨테이너 AWS 자격증명 (예시). docker/.env 로 복사해 실제 값을 채우세요.
#   cp docker/.env.example docker/.env
# docker/.env 는 .gitignore 처리됩니다. 실제 키를 커밋하지 마세요.
AWS_DEFAULT_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
